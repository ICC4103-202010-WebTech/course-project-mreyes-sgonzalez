<p id="notice"><%= notice %></p>

<table>
  <thead>
  <th>Name</th>
  <th>Description</th>
  <td><button class="btn btn-primary mx-4" id="edit_button_event">Edit</button></td>
  </thead>

  <tbody>
  <td><%= @organizations.name %></td>
  <td><%= @organizations.description %></td>


  </tbody>
</table>


<table>
  <h2>Organization List User</h2>
  <td><button class="btn btn-primary" id="add_button_event">Add</button></td>
  <tbody>
  <% @member.each do |member| %>
    <tr>
      <td><%= member.user.profile.name %></td>
    </tr>
  <% end %>
  </tbody>

</table>
<button class="btn btn-primary" id="back_button"><%= link_to 'Back', organizations_path %></button>