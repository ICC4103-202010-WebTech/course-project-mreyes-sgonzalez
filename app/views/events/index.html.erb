<p id="notice"><%= notice %></p>

<div class="row ml-0 mt-3" id="div-principal-text">
  <div>
    <h1>Events</h1>
    <p>In this section we can find all the events there are, we can see the title along with its description.
      Pressing each event leads us to the event page where we can see your photos and a greater amount of information
    </p>
    <p>
      We can create events with the add button
    </p>
  </div>

  <div class="d-flex justify-content-between align-items-center">
    <div class="btn-group">
      <button type="button" class="btn btn-outline-success" id="add-button"><%= link_to "Add", new_event_path%></button>
    </div>
  </div>
</div>


<div class="row">
  <% @events.each do |event| %>
    <div class="col-md-4" id="center-div-home">
      <div class="card mb-4 shadow-sm event-index-card">
        <% if event.image.attached?%>
          <%= link_to image_tag(event.image, class: "card-img-top"), url_for(event)%>
        <%else %>
          <%= link_to image_tag("thumbnail.svg", class: "card-img-top", width: "100%", background:
              "#55595c", color: "#eceeef"), url_for(event)  %>
        <%end %>
        <div class="card-body">
          <p class="card-text" id="title"><%= link_to event.title, event %></p>
          <p class="card-text"><%= event.description %></p>
          <% if current_user.admin%>
            <div class="btn-group">
              <button type="button" class="btn btn-sm btn-outline-secondary" id="view-edit-button"><%= link_to "View", event %></button>
              <button type="button" class="btn btn-sm btn-outline-secondary" id="view-edit-button"><%= link_to "Edit", edit_event_path(event) %></button>
            </div>
          <% elsif current_user%>
            <div class="btn-group">
              <button type="button" class="btn btn-sm btn-outline-secondary" id="view-edit-button"><%= link_to "View", event %></button>
            </div>
          <% else %>
            <div class="btn-group">
              <button type="button" class="btn btn-sm btn-outline-secondary" id="view-edit-button"><%= link_to "View", event %></button>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>